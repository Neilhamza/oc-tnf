#!/bin/sh
# Completion handler for kubectl/oc plugin discovery.
# Works regardless of whether the binary is installed as oc-tnf or kubectl-tnf.
if command -v oc-tnf >/dev/null 2>&1; then
  oc-tnf __complete "$@"
elif command -v kubectl-tnf >/dev/null 2>&1; then
  kubectl-tnf __complete "$@"
fi
